/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var twoSum = function(nums, target) {
  var map = new Map()
  map.set(target, '')

  for(var i = 0; i < nums.length; i++) {
    if (map.has(nums[i] + nums[i+1])) {
      return [i, i +1 ]
    }
  }

  return null
}